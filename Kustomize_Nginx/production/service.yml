apiVersion: v1
kind: Service
metadata:
  name: svc-argocd-nginx-prod
  namespace: argocd-nginx-prod
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - protocol:
      port: 80
      targetPort: 80
      nodePort: 30092